#include<iostream>
#include<bits/stdc++.h>
using namespace std;
int main()
{
    int j , t;
    cin>>t;
    for(j = 0 ; j<t ; j++)
    {
        int i , n , w;
        cin>>n>>w;
        int a[n] , b[n],c[n] , d[n];
        for(i =0 ; i< n ; i++)
        {
            cin>>a[i]>>b[i]>>c[i];

        }

        for(i =0 ;i<n ; i++)
        {
            d[i]=a[i]*b[i];
        }

        for(i =0 ;i<n ; i++)
        {

        }
    }
}
